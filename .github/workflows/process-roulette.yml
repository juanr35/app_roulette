name: Process Roulette

on:
  schedule:
    # Ejecutar processRouletteData cada 10 minutos
    - cron: '*/10 * * * *'
  workflow_dispatch: # Permitir ejecución manual

jobs:
  process-roulette-data:
    if: github.event.schedule == '*/10 * * * *' || github.event_name == 'workflow_dispatch'
    runs-on: ubuntu-latest
    timeout-minutes: 3

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci --legacy-peer-deps

      - name: Process roulette data
        env:
          DATABASE_URL: ${{ secrets.DATABASE_URL }}
        run: |
          echo "🚀 Iniciando procesamiento de datos de roulette..."
          npx tsx scripts/process-roulette-data.ts
          echo "✅ Procesamiento completado"